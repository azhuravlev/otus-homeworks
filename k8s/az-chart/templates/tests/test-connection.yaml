apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "az-chart.fullname" . }}-test-connection"
  labels:
    {{- include "az-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "az-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
